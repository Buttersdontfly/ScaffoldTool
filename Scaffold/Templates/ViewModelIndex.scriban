// <auto-generated-by-scaffold />

// Roslyn treats a file whose first comment contains <auto-generated as generated
// code and disables the nullable context in it, whatever the project setting.
// Without this directive every string? below is CS8632.
#nullable enable

namespace {{ viewModelNamespace }};

public partial class {{ model.className }}
{
    public {{ searchModel.className }} Search { get; set; } = new();

    public IReadOnlyList<{{ listItemModel.className }}> Items { get; set; } = [];

    public int TotalCount { get; set; }

    public int Page => Search.Page;

    public int PageSize => Search.PageSize;

    public int TotalPages => PageSize <= 0 ? 1 : (int)Math.Ceiling(TotalCount / (double)PageSize);

    public bool HasPrevious => Page > 1;

    public bool HasNext => Page < TotalPages;

    public int FirstRowOnPage => TotalCount == 0 ? 0 : ((Page - 1) * PageSize) + 1;

    public int LastRowOnPage => Math.Min(Page * PageSize, TotalCount);
}
