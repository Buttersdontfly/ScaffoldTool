// <auto-generated-by-scaffold />

// Roslyn treats a file whose first comment contains <auto-generated as generated
// code and disables the nullable context in it, whatever the project setting.
// Without this directive every string? below is CS8632.
#nullable enable

using System.ComponentModel.DataAnnotations;

namespace {{ viewModelNamespace }};

/// <summary>
/// Read-only projection. Navigations resolve to their display column rather
/// than their key, because a details page shows the label a person recognises.
/// </summary>
public partial class {{ model.className }}
{
{{~ for k in model.keys ~}}
    public {{ k.csType }} {{ k.name }} { get; set; }
{{~ end ~}}
{{~ for f in model.fields }}

    [Display(Name = "{{ f.displayName }}")]
    public {{ f.csType }} {{ f.name }} { get; set; }{{ f.initializer }}
{{~ end }}
{{~ for c in model.cascades }}

    /// <summary>
    /// Rows that a delete would take with it. Shown on the confirmation page.
    /// </summary>
    public int {{ c.name }} { get; set; }
{{~ end }}
}
